OBJ = bool.o \
	  float.o \
	  integer.o \
	  print.o \
	  string.o \
	  utils.o \
	  groups.o

INCLUDE = -I./src/

lib: $(OBJ)
	ar -vcru libvars.a ./bin/*.o
	
%.o: ./src/%.c
	gcc -g -c $< $(INCLUDE) -o ./bin/$@

clean:
	rm -rf libvars.a
	rm -rf ./bin/*.o
